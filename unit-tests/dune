(env
 (_
  (env-vars
   (LFSCSIGS "../../src/lfsc/signatures"))))

(rule
 (target dune.inc)
 (mode promote)
 (deps
  generate_dune_rules.sh
  (glob_files *.cnf)
  (glob_files *.smt2))
 (action
  (with-stdout-to
   %{target}
   (run ./generate_dune_rules.sh %{deps}))))

(include dune.inc)
